import 'package:flutter/material.dart';

class Servives extends StatelessWidget {
  const Servives({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text('Servives'),
      centerTitle: true,
      backgroundColor: Colors.red,
    ),
  );
}